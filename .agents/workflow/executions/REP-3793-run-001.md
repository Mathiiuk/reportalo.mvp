# Reporte de Ejecución: REP-3793-run-001

## Identificación
- **Tarea**: REP-3793 — BUG | Implementar y validar anonimización real de rostros y patentes (Sprint 14)
- **Jira**: https://unlz2026.atlassian.net/browse/REP-3793
- **Rama**: `fix/REP-3793-anonimizacion-real-rostros-patentes` (base `staging` `37bb2dc`)
- **Fecha**: 2026-09-26
- **Estado**: Código y tests aprobados, `READY_FOR_PR`. La función productiva `quarantine-anonymize` **no se tocó**; el
  código nuevo corre en la copia `quarantine-anonymize-rep3793` (CiudadAR), usada solo por el preview de la rama. Primera
  prueba E2E real correcta (§3-ter). Según la regla de Done falta el resto de la matriz de QA y el despliegue definitivo.

---

## 1. Qué se encontró

`quarantine-anonymize` (v12 en CiudadAR) **nunca pixelaba**: detectaba, informaba zonas y guardaba la foto con solo el EXIF
quitado. Además:

- `btoa(String.fromCharCode(...buffer))` desborda la pila con cualquier foto real, así que Vision nunca llegaba a correr.
- Sin `GOOGLE_VISION_API_KEY` devolvía **dos zonas inventadas** fijas. En los logs del 25/09 (11 fotos procesadas) no hay
  ni un aviso de Vision: la función venía usando ese camino.
- Las coordenadas normalizadas se multiplicaban por 1000 y no por el ancho y alto reales.
- Un objeto `car` se trataba como patente (hubiera tapado el auto entero).
- El cliente mostraba «3 zonas detectadas» fijo mientras procesaba.

## 2. Qué se hizo, por bloque

| Bloque | Cambio | Archivos |
|---|---|---|
| 0 · Spike CPU | Función `rep3793-spike-cpu` desplegada **solo en el proyecto descartable**. 1600 px: 200–341 ms; 4032 px: 2 de 4 corridas con `546 WORKER_RESOURCE_LIMIT`. Decisión: ImageScript en la Edge Function, 1600 px en cliente, 2048 px máximo en servidor | `scripts/rep3793/spike-cpu/` |
| 1 · Tamaño en cliente | `prepareEvidenceImage`: rota, convierte a JPEG y reduce a 1600 px (JPEG 0,85) en una pasada de canvas; no agranda. Un solo punto de entrada para online y pendientes offline | `metadataSanitizer.js`, `quarantinePipelineService.js` |
| 2 · Detección | `vision.ts`: Base64 por partes, cajas en píxeles reales, solo `License plate` (el auto no se pixela), texto con formato de patente argentina (Mercosur y anterior, autos y motos, palabras partidas), fragmentos dentro de un vehículo, timeout de 12 s, clave en header y nunca en la URL. Sin zonas inventadas | `quarantine-anonymize/vision.ts` |
| 3 · Pixelado | `pixelate.ts`: margen de 12 %, bloques de como máximo 8 por lado y mínimo 10 px, promedio por bloque (irreversible). `protect.ts`: siempre re-codifica, quita EXIF antes de Vision y otra vez al final | `pixelate.ts`, `protect.ts`, `exif.ts` (`readJpegSize`), `index.ts` |
| 4 · Fail-safe | Servidor: ante cualquier falla no sube nada y responde `failSafeTriggered` + `reason` (503 Vision, 413 tamaño, 415 formato, 422 ilegible). Cliente: lee el `reason` de la respuesta de error, muestra «No pudimos proteger tu foto» con texto según el motivo (sin detalle técnico), *Reintentar protección* y *Cambiar foto*. Offline: el pendiente queda en la cola con `PROTECTION/retry` | `index.ts`, `quarantinePipelineService.js`, `ReportProcessingScreen.jsx` |
| 5 · Puesta en marcha | Secreto propio de Vision cargado por Matías. Guía de despliegue (con el orden obligatorio), matriz Q1–Q10 y herramienta `proteger-foto.ts` para el antes/después | `scripts/rep3793/LEEME.md`, `proteger-foto.ts` |

El emulador del servidor solo existe con `QUARANTINE_VISION_EMULATOR=true` **y** un `SUPABASE_URL` local. El del cliente
ya estaba limitado a desarrollo y tests (H-30).

## 3. Evidencia de Quality Gates

- `pnpm test` (Vitest): **65 archivos, 478 tests en verde**. Tests nuevos:
  - `QuarantineAnonymizeProtection.test.js` (27): Vision con respuestas controladas, pixelado y fail-safe del pipeline.
  - `EvidenceImageResize.test.js` (7): reducción en el cliente.
  - `ProtectionFailSafeClient.test.jsx` (7): motivo del fail-safe, pantalla y nombre de función configurable.
  - `UT-3793-OFF-01` en `H35PendingSyncFailFast.test.js`: el pendiente offline no se envía sin protección.
- `pnpm build`: correcto; el service worker se genera.
- `deno check supabase/functions/quarantine-anonymize/index.ts`: correcto.
- **Pipeline real con ImageScript** (Deno local, `proteger-foto.ts` con zonas fijas sobre una foto de 1600×1200): 375 ms,
  las dos zonas quedan en bloques de un solo color y el resto de la imagen no cambia. Una foto de 4032 px se rechaza con
  `image_too_large` sin abrirse.
- `pnpm test:bdd`: el repo no tiene ese script; los escenarios quedan en `.agents/workflow/features/REP-3793.feature`.
- `agt task:verify REP-3793`: `agt` no está instalado en el entorno de esta ejecución.

## 3-bis. Prueba desde la rama

Matías prueba desde la rama, no desde `staging`. Para no reemplazar la función que usan staging y producción, la app
acepta `VITE_QUARANTINE_FUNCTION` (solo nombres `quarantine-anonymize-*`; sin la variable no cambia nada) y se despliega
una copia `quarantine-anonymize-rep3793` que usa solo el preview de Vercel de la rama. Pasos en `scripts/rep3793/LEEME.md` §1-bis.

## 3-ter. Evidencia E2E en el preview de la rama (26/09/2026)

Proyecto Supabase `CiudadAR` (`yryuhyiujyignkdhiyua`). Horas en UTC. Las fotos no se versionan: se referencian por reporte
y ruta en `report-evidences`.

### Configuración usada

| Paso | Detalle | Verificación |
|---|---|---|
| Secreto de Vision | `GOOGLE_VISION_API_KEY` propio (clave de Google Cloud para Cloud Vision API), cargado por Matías | En un primer intento quedó con la clave de Gemini (mismo digest que `GEMINI_API_KEY`) y Matías cargó otra. En la Prueba 2 Vision respondió bien, así que la clave cargada está habilitada para Cloud Vision. Falta confirmar en `secrets list` que el digest ya no coincide con el de Gemini |
| Copia de la función | `quarantine-anonymize-rep3793` v1, `verify_jwt: false`, mismo código que la rama | Humo sin sesión y con token falso: `401 «Se requiere una sesión iniciada.»`; `OPTIONS`: 200 |
| Preview de Vercel | `VITE_QUARANTINE_FUNCTION=quarantine-anonymize-rep3793`, solo Preview, rama `fix/REP-3793-…` (`vercel env add`) | `vercel env ls preview`; redeploy `reportalo-pbvtiqeu4-maias-projects-62afe59b.vercel.app` |
| Función productiva | `quarantine-anonymize` sin cambios de código | Pasó de v12 a v15 por las tres cargas de secretos (cada `secrets set` crea versión); el hash del código es el mismo |

### Prueba 1 · reporte `224b2516-5a36-49f7-96bc-d424c936bb9a` (12:41), antes de apuntar el preview a la copia

- El preview todavía llamaba a `quarantine-anonymize` (función vieja): la foto quedó **sin pixelar**, 640×480, 47 KB y sin
  EXIF (`report-evidences/e9256b38-0e88-4322-93c3-97851c55eb23/1790426477710_sanitized.jpg`).
- Sirve como evidencia del bug original. A las 12:32:59 la función vieja registró
  `Fallo en consulta a Google Vision API … RangeError: Maximum call stack size exceeded`: con la clave ya cargada, el
  paso a Base64 de REP-2404 desborda y la función cae a las zonas inventadas.

### Prueba 2 · reporte `dde681c3-f615-42e3-972c-4051f4e91370` (12:57), preview → copia nueva

Foto de celular: auto de frente con la patente legible y un conductor detrás del parabrisas.

Log de `quarantine-anonymize-rep3793` (12:57:55):

```json
{"event":"evidence_protected","clientSideId":"8b9dbbb9-9ca0-4d56-b0e8-25c786207efa",
 "storedPath":"8b9dbbb9-9ca0-4d56-b0e8-25c786207efa/1790427474568_sanitized.jpg",
 "width":900,"height":1600,"faces":0,"plates":2,
 "zones":[{"x":319,"y":988,"width":112,"height":56,"type":"license_plate"},
          {"x":373,"y":1002,"width":46,"height":31,"type":"license_plate"}],
 "emulated":false,"ms":2888}
```

| Criterio del ticket | Resultado |
|---|---|
| 1 · Detección real | ✅ Patente: dos zonas, una de Vision (objeto «License plate») y otra del texto leído dentro del vehículo. Las coordenadas caen sobre la patente en la foto real. **Rostro: no se probó un caso de frente** (ver falso negativo abajo) |
| 2 · Transformación real | ✅ Patente pixelada en bloques e ilegible; auto, logo y resto de la escena sin cambios (revisado sobre la foto guardada y un recorte ampliado de la zona) |
| 3 · Persistencia segura | ✅ `report-evidences` guarda solo la versión pixelada (345 KB); la fila de `report_images` apunta a ese archivo; el original se purgó de cuarentena (12:57:55) |
| 4 · Metadatos | ✅ La foto guardada no tiene EXIF: solo APP0 (JFIF) y las tablas del JPEG (DQT, DHT, SOF0) |
| Bloque 1 · tamaño | ✅ Guardada a 900×1600: el cliente redujo la foto vertical a 1600 px de lado mayor |
| Tiempo en el servidor | 2,9 s de punta a punta (Vision, apertura, pixelado y guardado) |

**Falso negativo registrado (caso Q3 de la matriz):** el conductor, detrás del parabrisas, en sombra y con una cara de
unos 40 px, **no se detectó** (`faces: 0`) y quedó sin pixelar. No es un error del pipeline sino un límite de la
detección de rostros de Vision. Opción a evaluar (no implementada, cambia el alcance): cuando Vision devuelve un objeto
«Person», pixelar la parte superior de su caja aunque no haya rostro detectado. Queda para decisión del equipo junto con
el resultado de la matriz de QA.

## 4. Pendiente para Done (no se cumple solo con código)

1. **Despliegue en orden:** frontend primero (staging y, si comparte Supabase, producción), después la función. Si la
   función va primero, los reportes con foto de celular fallan con `image_too_large`.
2. **Resto de la matriz de QA** (Iván): Q1–Q2 (rostro de frente), Q3 y Q7 (anotar falsos negativos como el del conductor),
   Q5–Q6, Q8–Q9 (fail-safe con Vision caído, que con la copia no corta a nadie) y Q10. Q4 (patente cercana) y los
   criterios 1 a 4 del ticket para patente quedaron cubiertos en §3-ter.
3. **Decidir sobre los rostros no detectados** (regla «Person» de §3-ter) y **limpiar al terminar**: borrar
   `quarantine-anonymize-rep3793` y la variable de Vercel después del despliegue definitivo.
4. **Validar con PO/Líder Técnico** el uso de Google Vision (servicio externo): las fotos se le envían ya sin EXIF.
5. **Rotar la clave de Gemini:** quedó expuesta durante la configuración. Actualizar `GEMINI_API_KEY` en Supabase y el `.env` local.
6. **Texto legal:** `termsService.js` §2.2 dice «difuminado irreversible». El pixelado cumple esa función, pero cambiar
   el texto requiere nueva versión de términos; no se tocó.
7. `Hora Trabajada Real` en Jira.

## 5. Fuera de alcance

- Usar la imagen como entrada del RAG (continuación de REP-3790): ticket aparte, propuesto al PM, bloqueado por este.
- `EvidencePreviewScreen.jsx` todavía dibuja zonas de ejemplo, pero no se usa en la app (solo en tests).
