# Reporte de Ejecución: REP-3471 (Run 001)

## 1. Información General
- **ID de Tarea:** REP-3471
- **Título:** Implementar script SQL/seed del MVP en Supabase
- **Fecha de Ejecución:** 04 de Septiembre de 2026
- **Responsable:** Matías Krepchuk
- **Rama:** `feat/REP-3471_script_sql_seed_supabase`
- **Estado Final:** READY_FOR_PR

---

## 2. Resumen de Cambios Realizados
1. **Definición DDL Idempotente (`supabase/schema.sql`):**
   - Definición de tablas relacionales: `profiles`, `report_categories`, `organismos`, `terms_consents`, `reports`, `report_evidences`.
   - Restricciones de integridad referencial (`FOREIGN KEY`, `CHECK`, `NOT NULL`) e índices de búsqueda espacial y relacional (`idx_reports_coords`, `idx_reports_category`, `idx_reports_status`, `idx_reports_jurisdiction`).
   - Políticas de seguridad Row Level Security (RLS) habilitadas en todas las tablas para permitir lectura pública consultiva y mutación autenticada.

2. **Dataset de Prueba Reproducible (`supabase/seed.sql`):**
   - Inserción idempotente (`ON CONFLICT`) de:
     - 3 perfiles ficticios sin PII (`ciudadano.demo@reportalo.ar`, `operador.caba@reportalo.ar`, `operador.avellaneda@reportalo.ar`).
     - 4 categorías oficiales según User Journey v2 (`infraestructura_vial`, `infraccion_transito`, `medio_ambiente`, `comercio_irregular`).
     - 6 organismos receptores oficiales para CABA y Avellaneda.
     - Consentimiento vigente v1.3 para el usuario de demo.
     - 8 reportes georreferenciados en CABA (Obelisco, Almagro, Belgrano, Palermo) y Avellaneda (Centro, Piñeyro, Crucecita, Sarandí) cubriendo los estados `Enviado`, `En curso` y `Resuelto`.
     - Evidencias fotográficas vinculadas y marcadas como sanitizadas (`approved`).

3. **Scripts de Utilidad & Documentación (`supabase/reset.sql`, `supabase/README.md`):**
   - Script de truncado en cascada (`TRUNCATE ... CASCADE`) para reseteo limpio del entorno de pruebas.
   - Guía operativa para ejecución en Supabase Studio y Supabase CLI.

4. **Suite de Pruebas Automatizadas (`src/test/SupabaseSeedValidation.test.js`):**
   - 6 casos de prueba unitaria en Vitest (`UT-SEED-01` a `UT-SEED-06`) que validan la existencia de archivos DDL, congruencia con `DEFAULT_REPORT_CATEGORIES`, límites del Bounding Box geográfico de CABA/Avellaneda, estados de reportes, versión de términos e idempotencia.

---

## 3. Evidencia de Quality Gates
- **Vitest Unit & Integration Tests:** 14/14 test suites passed (70/70 tests passed).
- **Vite Production Build:** Compilación limpia completada sin errores (`✓ built in 15.78s`).

---

## 4. Conclusión
Todos los criterios de aceptación de la tarea REP-3471 han sido cumplidos rigurosamente y el incremento está listo para revisión y PR.
