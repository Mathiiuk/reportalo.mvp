# Implementation Plan — H-35B

## 1. Resumen

Reordenar la tarjeta (encabezado + cuerpo a ancho completo) y agregar «Confirmar ubicación» reusando el modal del asistente.

## 2. Repositorio inspeccionado

- Stack: React 18 + Vite + Tailwind
- Runtime: Node · Package manager: pnpm
- Branch base: `staging` (`9708c99`)
- CI actual: Vitest

## 3. Cambios propuestos

### [NEW]
- `src/test/H35bPendingLayoutLocation.test.jsx`

### [MODIFY]
- `src/pages/PendingReportsPage.jsx`: tarjeta en dos bloques; botón y overlay con `AdjustLocationModal`; `retryAfterFix` compartido
- `src/services/pendingSyncService.js`: `getDraftProblems`; `validateDraftForSync` usa el primero
- `src/test/H35PendingStuckDrafts.test.jsx`: el mock usa las reglas reales

### [DELETE]
- Ninguno

## 4. Estrategia de implementación

TDD: 11 tests nuevos vistos en rojo (10 fallaban) y luego en verde. La lista de problemas se extrae del servicio sin cambiar su comportamiento (los tests de H-35 siguen en verde).

## 5. Migraciones / datos

Ninguna. La ubicación se guarda con `updateDraftReport` (H-35).

## 6. Seguridad

Sin cambios. La ubicación pasa por las mismas reglas del modal (localidad real elegida).

## 7. Observabilidad

Sin cambios.

## 8. Compatibilidad / rollback

Solo código: revertir el commit.

## 9. Plan de verificación

- [ ] Lint
- [ ] Typecheck
- [x] Unit (`pnpm test`: 415 tests)
- [ ] Integration
- [ ] E2E
- [x] Build (`pnpm build`)
- [ ] Security
- [ ] Smoke (manual, en el teléfono)

## 10. Definition of Done

Tests y build en verde y prueba visual de Matías en el teléfono.

## 11. Aprobación requerida

- [x] No requerida

## 12. Notas de ejecución

Ver `.agents/workflow/executions/H-35B-run-001.md`.
