# Implementation Plan — REP-3786

## 1. Resumen

Experimento controlado de optimización del RAG: 8 configuraciones × 9 casos × 5 repeticiones (360 generaciones reales), con la regla de decisión
fijada antes de correr. No modifica producción.

## 2. Repositorio inspeccionado

- Stack: React 18 + Vite; Supabase (Edge Function `analizar-reporte`, Postgres con pgvector); Gemini (`gemini-3.8-flash`)
- Runtime: Node (arnés local)
- Package manager: pnpm
- Branch base: `staging` (`b806eac`)
- CI actual: Vitest

## 3. Cambios propuestos

### [NEW]
- `scripts/rag-local-dev/rep3786/` (arnés, rúbrica, análisis y evidencia cruda)
- `docs/REP-3786_experimento-optimizacion-rag.md`

### [MODIFY]
- Ninguno (la Edge Function y `src/` no se tocan)

### [DELETE]
- Ninguno

## 4. Estrategia de implementación

1. Protocolo pre-registrado (regla R1–R5) commiteado antes de correr (`58ebbfe`).
2. Fase 0 sin LLM: recuperación k=6 de solo lectura contra CiudadAR, con validación por md5 de cada vector y del texto de cada fragmento.
3. Arnés + rúbrica commiteados antes de la primera generación (`7b89c0b`).
4. Sonda de thinking y corrida completa (360 llamadas), reanudable y con cada resultado guardado al momento.
5. Análisis con la regla pre-registrada y lectura sustantiva de R3.

## 5. Migraciones / datos

Ninguna. Solo lecturas sobre el corpus.

## 6. Seguridad

La clave de Gemini se lee del `.env` y no se imprime ni se versiona. Sin acceso de escritura a producción.

## 7. Observabilidad

Cada corrida deja una línea en `raw/runs.ndjson` con estado, citas, tokens, latencia y texto.

## 8. Compatibilidad / rollback

Sin impacto en producción: revertir el commit elimina scripts y documentación.

## 9. Plan de verificación

- [ ] Lint
- [ ] Typecheck
- [x] Unit (`pnpm test`, sin cambios en `src/`)
- [ ] Integration
- [ ] E2E
- [ ] Build
- [ ] Security
- [x] Experimento: 360/360 corridas, 0 errores de API

## 10. Definition of Done

Tabla comparativa, recomendación, evidencia versionada y parámetros finales documentados; decisión de adopción del equipo.

## 11. Aprobación requerida

- [x] No requerida para el experimento
- [ ] La adopción de cualquier cambio en producción requiere tarea y aprobación aparte

## 12. Notas de ejecución

Ver `.agents/workflow/executions/REP-3786-run-001.md`.
