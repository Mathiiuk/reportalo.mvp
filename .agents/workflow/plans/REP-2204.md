# Implementation Plan — REP-2204

## 1. Resumen

Botón de envío disponible solo con los datos mínimos, protegido contra el doble envío, con la categoría siempre
resuelta y un mensaje de error comprensible.

## 2. Repositorio inspeccionado

- Stack: React 18 + Vite + Tailwind; Supabase
- Runtime: Node
- Package manager: pnpm
- Branch base: `staging` (`2f34a70`)
- CI actual: Vitest

## 3. Cambios propuestos

### [NEW]
- `src/services/reportReadiness.js`
- `src/test/ReportReadiness.test.js`, `src/test/ResolveServiceId.test.js`

### [MODIFY]
- `src/components/report/ReportReviewStep.jsx`
- `src/pages/NewReportPage.jsx`
- `src/services/reportSubmissionService.js`, `categoriesService.js`, `pendingSyncService.js`
- `src/test/ReportReviewStep.test.jsx`, `ReportSubmissionService.test.js`, `EvidenceCaptureFlow.test.jsx`

### [DELETE]
- Ninguno

## 4. Estrategia de implementación

TDD: regla de datos mínimos → botón → servicios → integración en la página. Cada test nuevo se vio en rojo antes de
implementar. La prueba de doble toque se validó con una mutación (sin el candado falla con 2 llamadas).

## 5. Migraciones / datos

Ninguna.

## 6. Seguridad

Los mensajes al ciudadano no incluyen nombres de tablas ni de políticas; el detalle técnico queda en `error` para el log.

## 7. Observabilidad

Sin cambios: `console.error` conserva el detalle técnico.

## 8. Compatibilidad / rollback

Solo código: revertir el commit.

## 9. Plan de verificación

- [ ] Lint
- [ ] Typecheck
- [x] Unit (`pnpm test`)
- [ ] Integration
- [ ] E2E
- [x] Build (`pnpm build`)
- [ ] Security
- [ ] Smoke (manual, en el `staging` desplegado)

## 10. Definition of Done

Tests y build en verde, documentación completa y smoke manual del envío en `staging`.

## 11. Aprobación requerida

- [x] No requerida
- [ ] Requerida

## 12. Notas de ejecución

Ver `.agents/workflow/executions/REP-2204-run-001.md`.
