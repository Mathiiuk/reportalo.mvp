# Implementation Plan — REP-2203

## 1. Resumen

Regla única de descripción (10–280) usada por el formulario y el servicio, más un CHECK en la base.

## 2. Repositorio inspeccionado

- Stack: React 18 + Vite + Tailwind; Supabase
- Runtime: Node
- Package manager: pnpm
- Branch base: `staging` (`09c7e6f`)
- CI actual: Vitest

## 3. Cambios propuestos

### [MODIFY]
- `src/components/report/ReportDetailsStep.jsx`
- `src/services/reportSubmissionService.js`
- `supabase/schema.sql`
- `src/test/ReportDetailsStep.test.jsx`, `ReportSubmissionService.test.js`, `OfflineReportFlow.test.jsx`

### [NEW]
- `src/services/reportDescription.js`
- `src/test/ReportDescription.test.js`
- `supabase/migrations/20260923120000_rep2203_descripcion_reporte_limite.sql`

### [DELETE]
- Ninguno

## 4. Estrategia de implementación

TDD: tests de la regla, luego servicio, luego componente; se ajustan los tests de flujo que avanzaban sin descripción.

## 5. Migraciones / datos

`ALTER TABLE ... ADD CONSTRAINT ... CHECK (...) NOT VALID`. Los históricos se validan después con `VALIDATE CONSTRAINT`.

## 6. Seguridad

Validación en tres capas (UI, servicio, BD): el cliente no es de confianza.

## 7. Observabilidad

Sin cambios.

## 8. Compatibilidad / rollback

`DROP CONSTRAINT citizen_reports_description_length` revierte la base; el resto es código.

## 9. Plan de verificación

- [ ] Lint
- [ ] Typecheck
- [x] Unit (`pnpm test`)
- [ ] Integration
- [ ] E2E
- [x] Build (`pnpm build`)
- [ ] Security
- [ ] Smoke (manual en navegador, pendiente)

## 10. Definition of Done

Tests en verde, build correcto, spec/plan/tests completos y ejecución registrada.

## 11. Aprobación requerida

- [x] No requerida
- [ ] Requerida

## 12. Notas de ejecución

Ver `.agents/workflow/executions/REP-2203-run-001.md`.
