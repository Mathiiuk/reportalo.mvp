# Plan de Implementación: REP-3532

## 1. Módulos y Componentes

1. **`src/services/termsService.js`:**
   - Importar `supabase` y `isSupabaseConfigured` desde `../lib/supabaseClient`.
   - Modificar `recordTermsAcceptance` para hacer insert asíncrono en `terms_consents` y persistir en `localStorage`.
   - Crear `syncTermsConsentWithRemote(userId)` para consultar el registro más reciente en Supabase y actualizar el estado local.

2. **`src/context/AuthContext.jsx`:**
   - Sincronizar en segundo plano el consentimiento al detectar una sesión activa (`SIGNED_IN` / `initialSession`).

3. **`src/pages/TermsAndPermissionsPage.jsx`:**
   - Actualizar `handleFinishPermissions` y `handleSkipPermissions` para esperar (`await`) la resolución de `recordTermsAcceptance`.

4. **`src/test/TermsFlow.test.jsx`:**
   - Casos de prueba automatizados para validar llamadas asíncronas a Supabase y fallback offline.

---

## 2. Quality Gates
- `pnpm test`: 100% de tests en verde.
- `pnpm run build`: compilación de producción limpia.
