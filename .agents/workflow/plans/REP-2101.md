# Plan de Implementación: REP-2101

## 1. Módulos y Archivos a Modificar / Crear

1. **`src/context/AuthContext.jsx` & `src/hooks/useAuth.js`:**
   - Agregar método `signInWithMagicLink(email)` que invoque `supabase.auth.signInWithOtp`.
   - Soporte para captura de redirección por Magic Link y errores `otp_expired`.

2. **`src/pages/LoginPage.jsx`:**
   - Activar formulario de correo con validación en tiempo real.
   - Disparar `signInWithMagicLink` y al completarse navegar a `/check-email` pasando el correo en el estado de navegación.

3. **`src/pages/CheckEmailPage.jsx` [NUEVO]:**
   - Pantalla de confirmación "Revisá tu correo" con fidelidad visual 100% fiel al diseño móvil proporcionado y layout split-screen para desktop.
   - Lectura dinámica del email (desde `location.state` o query param).
   - Botón `"Abrir mi correo"` con apertura de cliente de correo (`mailto:` / webmail).
   - Temporizador de reenvío con cuenta regresiva (60 segundos) y acción interactiva de reintento.
   - Alerta informativa de caducidad (15 minutos).

4. **`src/App.jsx`:**
   - Registrar la ruta `/check-email`.

5. **`src/test/AuthFlow.test.jsx`:**
   - Matriz de pruebas unitarias para Magic Link: envío exitoso, validación de correo, temporizador de reenvío, renderizado fiel de CheckEmailPage, y manejo de errores.

---

## 2. Plan de Pruebas Automatizadas
- **UT-ML-01:** Validación de formato de email en formulario de acceso.
- **UT-ML-02:** `signInWithMagicLink` llama a `supabase.auth.signInWithOtp` con el correo y redirectTo correspondiente.
- **UT-ML-03:** Renderizado dinámico de `CheckEmailPage` con el correo real del usuario.
- **UT-ML-04:** Temporizador de cuenta regresiva y botón de reenvío funcional.
- **UT-ML-05:** Captura y notificación adecuada ante error de enlace inválido o expirado.

---

## 3. Quality Gates
- Ejecución de `pnpm test` (100% de tests en verde).
- Compilación de producción con `pnpm run build`.
