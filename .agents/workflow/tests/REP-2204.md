# Test Plan — REP-2204

## 1. Objetivo

Validar envío exitoso, validación de campos y fallo controlado (criterio de QA de la HU).

## 2. Riesgos a validar

- Enviar con datos incompletos.
- Un doble toque que repita el consentimiento o el guardado.
- Un reporte que se guarda sin categoría.
- Un mensaje técnico de la base llegando al ciudadano.

## 3. Unit tests

- [x] UT-RDY-01..06 `ReportReadiness.test.js`: datos mínimos y qué falta, en orden
- [x] UT-SND-01..06 `ReportReviewStep.test.jsx`: botón disponible, no disponible, ubicación, «Enviando…», sin conexión
- [x] UT-SNM-01..05 `ReportSubmissionService.test.js`: mensajes comprensibles, red, excepción, descripción
- [x] UT-SVC-01..04 `ResolveServiceId.test.js`: categoría resuelta por `service_code`, sin inventar

## 4. Integration tests

- [x] `UT-EVD-08` `EvidenceCaptureFlow.test.jsx`: envío exitoso; comprueba el payload exacto (solo categoría, descripción, localidad, coordenadas y estado)
- [x] `UT-SND-07`: dos toques → un consentimiento y un reporte (validado por mutación)

## 5. E2E tests

- [ ] No aplica (no hay E2E configurado)

## 6. Regression tests

- [x] Suite completa: 56 archivos, 371 tests

## 7. Security checks

- [x] Los mensajes al ciudadano no exponen `row-level`, `policy` ni nombres de tabla (UT-SNM-01)

## 8. Smoke tests

- [ ] Manual en `staging`: enviar con foto, categoría, descripción y localidad; comprobar el código del reporte
- [ ] Manual: probar el botón con la descripción vacía y con dos toques rápidos
- [ ] Manual: cortar la red antes de enviar y comprobar que el borrador sigue y el mensaje es claro

## 9. Evidencia requerida

- `pnpm test`, `pnpm build`; ver `executions/REP-2204-run-001.md`.

## 10. Resultado

`PENDING` hasta el smoke manual.
