# Test Plan — REP-2203

## 1. Objetivo

Validar descripción válida, vacía y límite de longitud (criterio de QA de la HU).

## 2. Riesgos a validar

- Que el paso 2 deje avanzar sin descripción y el envío falle recién al final.
- Que el servicio acepte texto fuera de rango si el formulario se saltea.
- Que el texto se pierda al navegar o en el borrador offline.

## 3. Unit tests

- [x] UT-DESC-01..07 `ReportDescription.test.js`: límites, vacía, solo espacios, 10, 280 y 281
- [x] UT-DESC-08..13 `ReportDetailsStep.test.jsx`: etiqueta, maxlength, contador, errores, continuar y conservación
- [x] `ReportSubmissionService.test.js`: rechaza corta/espacios/larga sin llamar a Supabase; recorta bordes

## 4. Integration tests

- [x] `OfflineReportFlow.test.jsx`: escribe una descripción válida al avanzar y restaura el borrador

## 5. E2E tests

- [ ] No aplica (no hay E2E configurado)

## 6. Regression tests

- [x] Suite completa: 50 archivos, 322 tests

## 7. Security checks

- [x] El servicio no confía en el formulario

## 8. Smoke tests

- [ ] Manual: paso 2 vacío, 5 caracteres, 280 y pegado de 300

## 9. Evidencia requerida

- `pnpm test` y `pnpm build`; ver `executions/REP-2203-run-001.md`.

## 10. Resultado

`PENDING` hasta el smoke manual.
