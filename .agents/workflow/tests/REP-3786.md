# Test Plan — REP-3786

## 1. Objetivo

Validar con corridas reales si alguna variante reduce tokens y latencia sin degradar precisión, trazabilidad ni abstención.

## 2. Riesgos a validar

- Que recuperar menos fragmentos haga perder normativa relevante.
- Que apagar el thinking degrade la calidad o no ahorre nada.
- Que `maxOutputTokens` 1024 trunque el JSON.
- Que las cifras de la propuesta (−45 % entrada, −57 % salida, 2.5× latencia) no se reproduzcan.

## 3. Unit tests

- [x] `pnpm test` sin cambios en `src/` (sin regresión)

## 4. Integration tests

- [x] Fase 0: recuperación de 9 casos contra CiudadAR; recall por top-k y umbral
- [x] Sonda de thinking: 4 modos sobre el mismo prompt

## 5. E2E tests

- [x] Experimento: 8 configuraciones × 9 casos × 5 repeticiones = 360 generaciones reales

## 6. Regression tests

- [x] Casos de REP-3764 / P-01 como base; los 6 que deciden dan 5/5 en todas las configuraciones (a nivel de norma)
- [x] Métrica adicional: citas del baseline a fragmentos fuera de top-3 (detectó la pérdida del art. 6.1.52 en el caso B)

## 7. Security checks

- [x] Solo lecturas sobre CiudadAR; clave de Gemini fuera del repo

## 8. Smoke tests

- [ ] No aplica

## 9. Evidencia requerida

`raw/runs.ndjson` (360 líneas), `raw/summary.json`, `raw/experiment.log`, `raw/retrieval-*.json`, `raw/thinking-mode.json`.

## 10. Resultado

`PASS` en ejecución técnica (360/360, 0 errores). La recomendación es **mantener el baseline**; ver el informe.
