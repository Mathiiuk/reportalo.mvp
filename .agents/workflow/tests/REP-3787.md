# Matriz de Pruebas — REP-3787

**Estrategia:** esta tarea no agrega lógica nueva; reemplaza la capa visual conservando props,
hooks, servicios y `data-testid`. Por lo tanto la verificación es **de no regresión**: la
suite existente debe seguir dando el mismo resultado que la baseline, bloque tras bloque.

## Baseline (staging `542ec57`, 21/09/2026)

| Gate | Resultado esperado |
|---|---|
| `pnpm test` | 266 tests · 266 verdes · sin rojos admitidos |
| `pnpm build` | Compila · PWA con SW propio · 53 entradas de precache |
No hay rojos admitidos. El merge de REP-3774 (PR #95) modificó `quarantinePipelineService.js`
y con eso quedaron corregidos UT-QPS-14 y UT-QPS-15, los dos fallos que el handoff reportaba
como preexistentes (H-06). La suite está 100 % en verde: cualquier rojo es una regresión.

## Verificación por bloque

| # | Verificación | Criterio de éxito |
|---|---|---|
| V-01 | `git apply --check` del parche | Aplica sin conflictos |
| V-02 | `pnpm test` | Sin rojos nuevos sobre la baseline |
| V-03 | `pnpm build` | Compila y genera el service worker |
| V-04 | Contraste contra las capturas del bloque | Claro y oscuro coinciden con el diseño a 390 px |
| V-05 | Recorrido manual del alta de reporte | Sin bloqueos en el flujo principal |
| V-06 | Estados offline y sincronización | Sin cambios de comportamiento |
| V-07 | Diferencias postergadas | Documentadas en el reporte de ejecución |

## Suites sensibles a los bloques 0 y 1

Tocan los archivos que estos bloques modifican y merecen atención especial:

- `NewReportFlow.test.jsx`
- `EvidenceCaptureFlow.test.jsx`
- `LocationFlow.test.jsx`
- `AdjustLocationModal.test.jsx`
- `OfflineReportFlow.test.jsx`
- `ReportReviewStep.test.jsx`

## Nota de entorno

Verificar en un checkout con `pnpm install` propio. Un `node_modules` enlazado desde otra
ruta hace que Vite rechace resolver `maplibre-gl` (`Denied ID ...`) y produce seis fallos de
colección que **no** son regresiones reales.
