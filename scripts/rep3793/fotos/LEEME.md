Carpeta local para las fotos de la matriz de QA de REP-3793. No se versiona (ver `../.gitignore`).
Las salidas de `proteger-foto.ts` (`*.protegida.jpg`, `*.zonas.jpg`, `*.resultado.json`) se generan acá al lado de cada foto.
