# Implementation Plan — REP-3310

## 1. Resumen

Mover el flag de onboarding de localStorage a Supabase user_metadata para persistencia en la DB, con sincronización automática para usuarios existentes.

## 2. Repositorio inspeccionado

- Stack: React 18, Vite 6, Supabase Auth
- Branch base: feature/design-system-uiux
- CI: Vitest + Vite build

## 3. Cambios propuestos

### [MODIFY]
- `src/pages/PermisosPage.jsx` — Importar useAuth y supabase. Crear `saveOnboardingToSupabase()`. Llamarla en handleContinue y handleSkip antes de setCompleted().
- `src/pages HomePage.jsx` — Importar supabase y user. Verificar `user.user_metadata.onboarding_completed` antes de localStorage. Sincronizar si flag está solo en localStorage.

## 4. Estrategia de implementación

1. PermisosPage: guardar flag en Supabase metadata al completar onboarding
2. HomePage: verificar metadata de Supabase primero, luego localStorage
3. Sincronización automática si flag está en localStorage pero no en Supabase

## 5. Migraciones / datos

No aplica (Supabase crea el campo automáticamente)

## 6. Seguridad

- Flags almacenados en user_metadata (no sensible)
- Sin impacto en tokens o sesiones

## 7. Observabilidad

- Sin cambios

## 8. Compatibilidad / rollback

- Usuarios pre-cambio: se sincroniza on-demand
- Rollback: revert commit

## 9. Plan de verificación

- [x] Unit tests (npx vitest run) — 3/3 pasan
- [x] Build (npx vite build) — exitoso

## 10. Definition of Done

- [x] Todos los AC completados
- [x] Build exitoso
- [x] Tests pasan

## 11. Aprobación requerida

- [x] No requerida

## 12. Notas de ejecución

- El flag se guarda en `user.user_metadata.onboarding_completed` (Supabase upsert automático)
- localStorage se mantiene como fallback rápido para la primera carga
