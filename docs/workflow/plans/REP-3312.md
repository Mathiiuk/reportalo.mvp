# Implementation Plan — REP-3312

## 1. Resumen

Hardening de seguridad para Login con Google: integrar redirect validation, agregar CSP y HSTS headers.

## 2. Cambios

### [MODIFY]
- `src/services/authService.js` — `signInWithGoogle()`: validar `getSecureRedirectUrl()` contra `validateRedirectUrl()` antes de llamar a `signInWithOAuth`. Si falla, lanzar error.
- `vercel.json` — Agregar headers `Content-Security-Policy` y `Strict-Transport-Security`.

## 3. Estrategia

1. authService: validar redirect URL en signInWithGoogle
2. vercel.json: agregar CSP + HSTS
3. Build + tests

## 4. Seguridad

- CSP previene XSS y exfiltración de tokens
- HSTS fuerza HTTPS
- Redirect validation previene open redirect attacks

## 5. Verificación

- [x] Build exitoso
- [x] Tests pasan
- [x] Flujo OAuth no roto
