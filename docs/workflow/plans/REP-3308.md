# Implementation Plan — REP-3308

## 1. Resumen

Auditoría completa del frontend contra master-workflow, con corrección de bugs, mejora de consistencia Tailwind, y agregado de patrones de robustez (ErrorBoundary, loading states).

## 2. Repositorio inspeccionado

- Stack: React 18, Vite 6, Tailwind CSS 3.4, Framer Motion, MapLibre GL JS
- Runtime: Node.js
- Package manager: pnpm
- Branch base: feature/design-system-uiux
- CI actual: Vitest + Vite build

## 3. Cambios propuestos

### [MODIFY]
- `src/utils/supabase.js` — Eliminar fallbacks hardcoded
- `src/main.jsx` — Agregar SW registration (luego eliminado)
- `src/styles/index.css` — Eliminar import duplicado de MapLibre
- `src/routes/index.jsx` — Corregir ruta /permisos + comentarios
- `src/context/OnboardingContext.jsx` — Validar valores de localStorage
- `src/pages/HomePage.jsx` — Migrar inline styles a Tailwind
- `src/pages/OnboardingPage.jsx` — Migrar inline styles a Tailwind
- `src/pages/TermsPage.jsx` — Migrar inline styles + loading state
- `src/pages/PermisosPage.jsx` — Corregir imagen + loading state
- `src/pages/MapaPage.jsx` — Fix marker leak
- `src/components/permisos/PermisoCard.jsx` — Button semántico
- `src/components/common/ErrorBoundary.jsx` — Nuevo componente
- `public/manifest.json` — Campos id, scope, shortcuts
- `tailwind.config.js` — Fuente Manrope
- `src/test/ReportaloV2Flow.test.jsx` — Tests actualizados

### [DELETE]
- `src/pages/PermissionsPage.jsx` — Obsoleta (reemplazada por PermisosPage)
- `public/sw.js` — Eliminado (causaba cache stale)

## 4. Estrategia de implementación

Fase 1: Seguridad y PWA → Fase 2: Arquitectura → Fase 3: Consistencia → Fase 4: Accesibilidad

## 5. Migraciones / datos

No aplica (sin cambios de schema)

## 6. Seguridad

- Credenciales Supabase ya no hardcoded
- Service Worker eliminado (causaba problemas)

## 7. Observabilidad

- ErrorBoundary captura errores de render y los loggea

## 8. Compatibilidad / rollback

- Revert del commit en rama feature

## 9. Plan de verificación

- [x] Unit tests (npx vitest run) — 3/3 pasan
- [x] Build (npx vite build) — exitoso

## 10. Definition of Done

- [x] Todos los AC completados
- [x] Build exitoso
- [x] Tests pasan
- [x] Sin regresiones

## 11. Aprobación requerida

- [x] No requerida

## 12. Notas de ejecución

- El Service Worker se eliminó porque causaba cache de chunks stale en Vercel
- La migración de inline styles se hizo en HomePage, OnboardingPage y TermsPage (las páginas principales con más inline styles)
