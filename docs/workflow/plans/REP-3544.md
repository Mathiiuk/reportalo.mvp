# Plan de Implementación: REP-3544

## 1. Módulos y Archivos a Modificar

1. **`src/pages/TermsAndPermissionsPage.jsx` [MODIFY]:**
   - Incorporar estado `showTermsError` (boolean).
   - Actualizar cabecera para renderizar el badge `VIGENTE` (`#E3F5EC` / `#2C7A55`) y versión `1.2 · desde 08/2026`.
   - Modificar `handleAcceptTerms`:
     - Si `!acceptedTerms`: `setShowTermsError(true)`.
     - Si `acceptedTerms`: avanzar a `currentStep = 2`.
   - Modificar checkbox:
     - `onChange`: alternar `acceptedTerms` y si se tilda a `true`, resetear `showTermsError(false)`.
     - Clase dinámica de borde: `showTermsError && !acceptedTerms ? 'border-2 border-[#E07C1A]' : (acceptedTerms ? 'bg-[#1E6FCB] text-white' : 'border-2 border-[#CFD8E2] bg-white')`.
   - Renderizado condicional del banner de error `#FFF2E0` con icono `error` y texto `La casilla es obligatoria. Marcala para poder continuar.`.
   - Actualizar botón "Aceptar y continuar":
     - Estado no tildado: fondo `#DDE6EF`, texto `#9AA7B5`, `onClick={handleAcceptTerms}` (dispara error).
     - Estado tildado: fondo `#1E6FCB`, texto blanco, sombra interactiva.
   - Añadir botón `"Rechazar"` en la sección inferior con manejador `handleRejectTerms`:
     - Notifica con `toast.info`.
     - Redirige a `/app`.

2. **`src/App.jsx` [MODIFY]:**
   - Ajustar `ProtectedRoute` para que usuarios autenticados que hayan completado Onboarding puedan acceder a `/app` aunque aún no hayan aceptado los términos.

3. **`src/test/TermsFlow.test.jsx` [MODIFY]:**
   - Incorporar pruebas unitarias de validación para REP-3544:
     - Intento de aceptación sin checkbox -> despliegue de banner `#FFF2E0` y contorno `#E07C1A`.
     - Tildado de checkbox -> desaparición del banner y borde naranja.
     - Clic en "Rechazar" -> redirección a `/app` sin registro de consentimiento.
     - Exploración en `/app` sin consentimiento y bloqueo al pulsar "Crear nuevo reporte".

---

## 2. Quality Gates y Validaciones
- `pnpm test`: 100% de tests unitarios y de integración pasando.
- `pnpm run build`: compilación de producción limpia sin errores de build ni de tipos.
