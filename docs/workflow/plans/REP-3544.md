# Plan de Implementación: REP-3544

## 1. Módulos y Componentes

1. **`src/pages/TermsAndPermissionsPage.jsx`:**
   - Estados: `showTermsError` (boolean), `showRejectModal` (boolean).
   - Cabecera: Badge `VIGENTE` (`#E3F5EC` / `#2C7A55`) y versión `1.2 · desde 08/2026`.
   - Banner de advertencia condicional `#FFF2E0` con icono `error` y texto `La casilla es obligatoria. Marcala para poder continuar.`.
   - Borde de checkbox reactivo: contorno `#E07C1A` ante error de validación.
   - Botón *"Rechazar"* al pie de la vista de términos.
   - Modal/Bottom Sheet de confirmación de rechazo:
     - Icono `block` en contenedor `#FDECEA`.
     - Título `"¿Rechazar los términos?"`.
     - Descripción de impacto legal.
     - Botón `"Rechazar y salir"` (`#C0392B`) y `"Volver a los términos"` (`#F2F5F9`).

2. **`src/App.jsx`:**
   - Ajuste de `ProtectedRoute` y `PublicRoute` para permitir exploración en `/app` a usuarios con onboarding completado.

3. **`src/test/TermsFlow.test.jsx`:**
   - Cobertura completa de casos de prueba para casilla desmarcada, alerta de error, apertura y confirmación del diálogo de rechazo, y exploración sin consentimiento.

---

## 2. Quality Gates
- `pnpm test`: 100% de tests unitarios e integración pasando.
- `pnpm run build`: compilación de producción limpia.
