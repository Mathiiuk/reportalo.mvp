# Plan de Implementación: REP-3544

## 1. Módulos y Componentes

1. **`src/services/termsService.js`:**
   - Constantes `CURRENT_TERMS_VERSION = '1.3'` y `TERMS_EFFECTIVE_DATE = '09/2026'`.
   - Helpers: `getTermsUpdateStatus`, `postponeTermsUpdate`, `isTermsConsentBlocked`, `formatAcceptedDate`.

2. **`src/pages/TermsAndPermissionsPage.jsx`:**
   - Barra superior institucional fija con logo (`20x26px`) y email de la usuaria.
   - Contenedor con scroll vertical fluido (`overflow-y-auto`) y anchos responsivos (`max-w-[480px] md:max-w-[560px] lg:max-w-[680px]`).
   - Título 30px en escritorio con bajada institucional.
   - 3 bloques informativos en una sola columna vertical en todos los anchos.
   - Pie fijo (`shadow-[0_-4px_16px_rgba(0,0,0,0.03)]`):
     - `< 1024px`: apilado con touch target mínimo de 44px.
     - `≥ 1024px`: horizontal (checkbox a la izquierda, botones *"Rechazar"* y *"Aceptar y continuar"* a la derecha).

3. **`src/App.jsx`:**
   - Blindaje de `ProtectedRoute` y `PublicRoute` con `isTermsConsentBlocked(user?.id)`.

4. **`src/test/TermsFlow.test.jsx`:**
   - Matriz completa de 29 pruebas (`UT-TM-01` a `UT-TM-29`).

---

## 2. Quality Gates
- `pnpm test`: 100% de tests en verde.
- `pnpm run build`: compilación de producción limpia.
