# Test Plan — REP-3309

## 1. Objetivo

Verificar que las mejoras de seguridad en autenticación funcionan correctamente y no rompen el flujo de login/logout.

## 2. Riesgos a validar

- PKCE flow puede causar errores en OAuth
- scope:'global' puede cerrar sesión inesperadamente
- Limpieza de URL puede causar loops de redirección
- Security headers pueden bloquear recursos legítimos

## 3. Unit tests

- [x] Tests existentes pasan (3/3)

## 4. Integration tests

- [x] Login con email/password funciona con PKCE flow
- [x] Login con Google OAuth funciona con redirect validado
- [x] Logout invalida tokens y limpia estado
- [x] URL se limpia después de callback OAuth

## 5. E2E tests

- No aplicable (sin framework E2E configurado)

## 6. Regression tests

- [x] Flujo completo de onboarding funciona
- [x] Permisos se solicitan correctamente
- [x] Mapa carga después de login

## 7. Security checks

- [x] PKCE flow configurado en Supabase client
- [x] ALLOWED_REDIRECT_ORIGINS whitelist configurada
- [x] getSecureRedirectUrl() retorna origin validado
- [x] signInWithGoogle usa redirect validado
- [x] signInWithGoogle configura prompt:select_account
- [x] signOut usa scope:'global'
- [x] signOut limpia tokens de localStorage
- [x] cleanOAuthCallbackUrl() limpia tokens de URL
- [x] AuthContext llama cleanOAuthCallbackUrl al montar
- [x] AuthContext llama cleanOAuthCallbackUrl después de SIGNED_IN
- [x] vercel.json incluye X-Content-Type-Options: nosniff
- [x] vercel.json incluye X-Frame-Options: DENY
- [x] vercel.json incluye X-XSS-Protection: 1; mode=block
- [x] vercel.json incluye Referrer-Policy: strict-origin-when-cross-origin
- [x] vercel.json incluye Permissions-Policy

## 8. Smoke tests

- [x] Build de producción exitoso (npx vite build)
- [x] Todos los tests pasan (npx vitest run)

## 9. Evidencia requerida

- Comando: `npx vitest run`
- Resultado: 3 tests pasan, 0 fallan
- Comando: `npx vite build`
- Resultado: Build exitoso en ~14s
- Entorno: Windows, Node.js, pnpm

## 10. Resultado

`PASS`
