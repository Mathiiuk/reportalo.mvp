# Matriz de Pruebas: REP-2302

| ID | Tipo | Caso de Prueba | Resultado Esperado | Estado |
|---|---|---|---|---|
| **UT-LS-01** | Unit / Service | `getUserCoordinates` con geolocalización exitosa | Retorna `status: 'GRANTED'`, `coordinates: [lng, lat]` y `accuracy` en metros | Aprobado |
| **UT-LS-02** | Unit / Service | `getUserCoordinates` ante error PERMISSION_DENIED (código 1) | Retorna `status: 'DENIED'` y coordenadas fallback `DEFAULT_CITY_COORDINATES` | Aprobado |
| **UT-LS-03** | Unit / Service | `getUserCoordinates` ante error POSITION_UNAVAILABLE (código 2) | Retorna `status: 'UNAVAILABLE'` y mensaje claro | Aprobado |
| **UT-LS-04** | Unit / Service | `getUserCoordinates` ante TIMEOUT (código 3) | Retorna `status: 'TIMEOUT'` sin bloquear la promesa | Aprobado |
| **UT-LS-05** | Unit / Permission | `getGeolocationPermissionState` con API Permissions | Retorna `granted`, `denied` o `prompt` según `navigator.permissions.query` | Aprobado |
| **UT-LS-06** | Unit / Watch | `watchUserCoordinates` suscripción y cancelación | Llama a `watchPosition` y permite desuscribirse con `clearWatch` | Aprobado |
| **UT-LS-07** | Unit / Hook | Hook `useGeolocation` actualización de estado reactivo | Expone `coordinates`, `accuracy`, `status`, `isLoading` y `refreshLocation` | Aprobado |
| **UT-LS-08** | Architecture | Cumplimiento estricto Anti-EXIF | La ubicación se deriva 100% de la API del navegador sin procesar EXIF de imágenes | Aprobado |
