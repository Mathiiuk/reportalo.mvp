name: Security

on:
  pull_request:
  push:
    branches: [main, staging]

permissions:
  contents: read

jobs:
  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Dependency / security scan
        run: 'echo "TODO: configure stack-specific security scanners"'
